// 一种自适应多重耗能桥墩防撞装置 —— 完整外观模型（含外包柔性膜）
// 打开后按 F5 预览；以下视口参数使模型自动居中并缩放到合适大小
$vpt = [0, 0, -2000];
$vpr = [60, 0, 40];
$vpd = 140000;  // 视口预设：打开后按 F5 即定位到模型


include <anticollision_lib.scad>

device();
